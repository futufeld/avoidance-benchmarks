// Number of scenarios to run for each benchmark.
#[allow(dead_code)]
pub const NUM_RUNS: u32 = 1_000;

// Number of batches of runs for each benchmark.
#[allow(dead_code)]
pub const NUM_BATCHES: u32 = 100;